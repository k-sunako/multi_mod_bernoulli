[project]
name = "multi-mod-bernoulli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sympy>=1.13.3",
]

[tool.uv]
dev-dependencies = [
    "python-lsp-server[all]>=1.12.0",
]
